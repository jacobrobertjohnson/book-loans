@model BorrowerFormViewModel
@{
    ViewData["Title"] = "Create Borrower";
}

<div class="page-title-row">
    <a class="back-arrow" asp-action="Borrowers" title="Back to Manage Borrowers">&#8592;</a>
    <h1 class="page-title">Create Borrower</h1>
</div>

<form asp-action="CreateBorrower" method="post" class="form-shell stack">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="message-error"></div>

    <h2>Borrower Details</h2>

    <div class="form-grid">
        <div class="field">
            <label asp-for="FirstName"></label>
            <input asp-for="FirstName" />
            <span asp-validation-for="FirstName"></span>
        </div>

        <div class="field">
            <label asp-for="LastName"></label>
            <input asp-for="LastName" />
            <span asp-validation-for="LastName"></span>
        </div>
    </div>

    <div class="actions-row">
        <button type="submit" class="button-primary">Save</button>
    </div>
</form>
